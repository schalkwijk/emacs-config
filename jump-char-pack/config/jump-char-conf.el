;; I don't use these because M-m I used to back to indentation and I don't feel like customizing a DWIM C-a keybinding
;; The actual keypresses that I have mapped to this behavior can be found in key-chord-mode pack

;; (global-set-key [(meta m)] 'jump-char-forward)
;; (global-set-key [(shift meta m)] 'jump-char-backward)
